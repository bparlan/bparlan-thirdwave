export interface ThemeAccent {
  primary: string;
  glow: string;
  bgBg: string;
  accent: string;
  nodeName: string;
  isDarkText: boolean;
}

export interface WalletAccent {
  primary: string;
  glow: string;
  accent: string;
}
